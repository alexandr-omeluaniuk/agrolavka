package ss.martin.security.constants;

/**
 * Entity permissions.
 * @author ss
 */
public enum EntityPermission {
    /** Create. */
    CREATE,
    /** Update. */
    UPDATE,
    /** Read. */
    READ,
    /** Delete. */
    DELETE,
    /** Activate. */
    ACTIVATE,
    /** Deactivate. */
    DEACTIVATE;
}
